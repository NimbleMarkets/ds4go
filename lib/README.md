# lib

Place your ds4 shared library here if you do not want to use `DS4_LIB`,
`DS4_DIR/lib`, or `-lib`.

Expected filenames:

- `libds4.dylib` on macOS
- `libds4.so` on Linux
- `libds4.dll` on Windows
